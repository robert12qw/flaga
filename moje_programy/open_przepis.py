if jezeli wybrałeś coś to cos
jeżeli budyń towyfrukuj 


def character( name):
    content = wiki.summary(name, sentences=6)
    return content


def open_przepis( przepis):
if przepis == budyn
    text_lines = open('dane/budyn.txt', encoding='utf-8').readlines()

    budyń_lines = []
    for line in text_lines:
        line = line.strip()
        budyń_lines.append(line)
    
    return budyń_lines